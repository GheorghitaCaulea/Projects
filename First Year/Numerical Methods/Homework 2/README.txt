CAULEA Gheorghita 314CB

CERINTA 1
	In inceputul rezolvarii deschid fisierul pentru citire. In cazul in care deschiderea s-a facut, citesc pe rand datele de intrare si le folosesc pentru construierea unei matrici de adiacenta. Pregatesc valoarea vectorului pentru pasul initial, initializandu-l cu '1/N'. Construiesc matricea ajutatoare 'M' dupa care execut algoritmul cat timp conditia este indeplinita, astel calculandu-se PageRank-ul.

CERINTA 2
	 In inceputul rezolvarii deschid fisierul pentru citire. In cazul in care deschiderea s-a facut, citesc pe rand datele de intrare si le folosesc pentru construierea unei matrici de adiacenta. Construiesc matricea ajutatoare 'M' si o
matrice unitate. Contruiesc matricea 'T' pe care voi aplica functia 'GramSchmidt'. Calculez PageRank-ul. Functia ajutatoare 'GramSchmidt' realizeaza factoarizarea Q R pentru o matrice data si apoi aplica SST pentru determinarea pe rand a coloanelor solutii.

CERINTA 3
	In inceputul rezolvarii deschid fisierul pentru citire. In cazul in care deschiderea s-a facut, citesc pe rand datele de intrare si le folosesc pentru construierea unei matrici de adiacenta. Initializez vectorul cu '1/N'. Construiesc matricea ajutatoare 'M', pregatesc o matrice 'T' pe care aplic metoda puterii directe cu scopul de a determina vectorul de valorii proprii. Vectorul de valorii proprii, normat, va contitui PageRank-ul.

CERINTA 4
	Calculez PageRank-ul pentru cele trei cerinte. Deschid fisierul pentru citire si in cazul in care deschiderea a reusit citesc datele de intrare cu scopul de a putea accesa ultimile doua valori. Scriu in fisier PageRank-urile. Ordonez folosind metoda bulelor PageRank-ul calculat pentru cerinta 2, afisez poziile initiale si finale si gradul de apartenenta pentru fiecare valoare din vector. Functia de 'Apartenenta' calculeaza gradul de apartenenta a unui element dupa cum este incadrat intre intervalele date.	 

